{
  "name": "wilfredo-caro-marca",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.47.0",
    "vite": "^8.0.4",
    "vite-plugin-pwa": "^1.3.0",
    "wrangler": "^4.114.0"
  },
  "dependencies": {
    "three": "^0.185.1"
  }
}
